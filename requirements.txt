# Core Pathway dependencies
pathway[xpack-llm,xpack-llm-docs]==0.26.1
python-dotenv~=1.0
mpmath~=1.3

# FastAPI and web server dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# OpenAI dependency for LLM functionality
openai>=1.0.0

# Additional dependencies for requests
requests>=2.31.0

# Redis for collaborative filtering and caching
redis>=5.0.0

# Machine learning for collaborative filtering
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# User identification and session management
user-agents>=2.2.0

# Realistic weather simulation (no API keys required)
# Weather data is simulated based on location, time, and seasonal patterns

# Simulated weather service for contextual recommendations
# No API keys required - uses realistic weather simulation based on location and time
